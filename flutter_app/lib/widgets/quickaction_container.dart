import 'package:flutter/material.dart';

class QuickActionContainer extends StatelessWidget {
  const QuickActionContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}